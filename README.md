# Leaving Cert Maths

A PreTeXt prototype for an Irish Leaving Certificate Mathematics textbook by Ronan Downes.

The first prototype chapter is **Algebra**, covering Ordinary and Higher Level material with a deliberately visual approach to factorisation.

## Build targets

- `pretext build web`
- `pretext build print`

The repository includes a GitHub Actions workflow that validates and builds the book automatically.
